#include "ScalarConverter.hpp"

int	main(int ac, char **av)
{
	if (ac != 2)
	{
		std::cout << "* two arguments only *" << std::endl;
		return 0;
	}
	ScalarConverter::convertType(av[1].c_str());
	return 0;
}
